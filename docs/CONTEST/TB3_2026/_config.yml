# Jekyll config for TB3_2026 GitHub Pages
# Only process actual website files, skip everything else
exclude:
  # ROS packages - not website content
  - autorace_idminer
  - architecture
  - research
  - tech
  - updates
  # Keep index.html, assets/, data/ as-is (Jekyll processes them normally)
